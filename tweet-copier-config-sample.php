<?php

/** This must be set to some random alphabetic string in order for logging to work. It's a poor man's security feature. */
$logfile_suffix = '';

/** Can be error, warn, info or debug. Otherwise, no logging. */
$log_level = 'info';
